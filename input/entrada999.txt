typedef struct Pessoa Pessoa;
typedef real frase;
typedef frase frase1;
typedef frase1 frase2;

struct Pessoa {
    var{
        string cpf, nome;
        int idade, peso, altura;
    }
}

typedef struct Pessoa alguem;

typedef int funcionario;

struct funcionario extends alguem{
    var{
        real salario;
    }
}

struct empregado extends Pessoa{
    var{
        real salario;
    }
}

struct empregado extends struct Pessoa{
    var{
        real salario;
    }
}

struct empregado extends serHumano{
    var{
        real salario;
    }
}

var{
    struct funcionario zop  = 10;
    struct funcionario zap, zip = zap;
    struct funcionario zup = zap;
    real w=2.3,i, esther;
    int carro[10][20]=[[1,2,3,4,5,3],[1,2,3,4,esther]];
 }

struct gerente extends w{
    var{
        real salario;
    }
}


typedef struct Pessoa Pessoa;

procedure soma(int a, int b){
    var{
        real teste=2.8+2, p, i, j;
     }

    p = 
    i;
    read(i, u[i]);
    if(!variavel) then{
       read(i);
    }
    i++;
    i--;
    --j;
    ++j;
    teste();
    while(!variavel){
	read(j);
    }

}

procedure teste(int a, int b){print("casa");}
procedure teste(int a, int b, int c){print("casa");}
procedure teste(int a, int b, int c, int d){print("casa");}

procedure teste(int a, int b){
    var{
        struct Pessoa xablau;
        frase2 soma;
        int i;
        string cadeia="zap";
    }
    xablau = true;
    soma = true;
    teste(1, 2, 3);
    read(i, u[i]);

}

procedure start(){
    var{
        struct Pessoa xablau;
    }
    read(i, u[i]);


}
